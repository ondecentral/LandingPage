export interface NFTCardProps {
  primaryImg: string;
  secondaryImg: string;
  title: string;
  description: string;
  owner: string;
}
export interface CollectionCardProps {
  img: string;
  title: string;
  description: string;
}
