export type Colors = {
  primary: string;
  secondary: string;
  primaryBackground: string;
  secondaryBackground: string;
  primaryGradient: string;
  secondaryText: string;
};
export type Fonts = {
  primary: string;
  secondary: string;
};
